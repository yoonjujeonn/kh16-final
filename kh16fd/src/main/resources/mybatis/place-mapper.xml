<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="place">

    <!-- 기존 sequence / insert / selectByDepth 유지 -->
    <select id="sequence" resultType="long">
        select place_seq.nextval from dual
    </select>

    <insert id="insert">
        insert into place(
            place_id, 
            place_depth1, 
            place_depth2, 
            place_depth3
        )
        values(
            #{placeId},
            #{placeDepth1},
            #{placeDepth2},
            #{placeDepth3}
        )
    </insert>

    <select id="selectByDepth" resultType="placeDto">
        select *
        from place
        where place_depth1 = #{depth1}
        and place_depth2 = #{depth2}
        and place_depth3 = #{depth3}
    </select>


    <!-- 상위지역 목록 -->
    <select id="upperPlaceList" resultType="string">
        SELECT DISTINCT place_depth1
        FROM place
        ORDER BY place_depth1 ASC
    </select>

    <!-- 하위지역 목록 -->
    <select id="lowerPlaceList" parameterType="string" resultType="string">
        SELECT DISTINCT place_depth2
        FROM place
        WHERE place_depth1 = #{upperPlace}
        ORDER BY place_depth2 ASC
    </select>

</mapper>
