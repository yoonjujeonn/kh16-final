<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="place">
  <select id="sequence" resultType="long">
  	select place_seq.nextval from dual
  </select>
  <insert id="insert">
  	insert into place
  	(
  	place_id, place_depth1, place_depth2, place_depth3
  	) 
  	values(
  	#{placeId}, #{placeDepth1}, #{placeDepth2}, #{placeDepth3}
  	)
  </insert>
  
  <select id="selectByDepth" resultType="placeDto">
  	select * from place 
  	where place_depth1 = #{depth1} 
  	and place_depth2 = #{depth2}
  	and place_depth3 = #{depth3}
  </select>
  
</mapper>